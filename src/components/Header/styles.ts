import styled from "@emotion/styled";

export const TelLink = styled("a")(({}) => ({
  textDecoration: "none",
  fontSize: "0.8rem",
  fontWeight: "bold",
  color: "inherit",
  textAlign: "center",
}));

export const SubLogo = styled("span")(({}) => ({
  fontFamily: "Caveat",
  textDecoration: "none",
  fontSize: "0.8rem",
  lineHeight: "12px",
  color: "inherit",
  textAlign: "center",
}));
